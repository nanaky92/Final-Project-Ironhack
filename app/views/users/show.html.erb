<%@groups.each do |group| %>
  <p>
    <%=link_to( group.name, "/groups/#{group.id}")%>
    <%=link_to("Exit group", "/groups/#{group.id}/delete_user")%>
    <%=link_to("Delete group", "/groups/#{group.id}/delete_group")%>
  </p>

<%end%>

<p><%=link_to( "Create group", "/groups/new")%></p>
